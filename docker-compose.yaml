services:

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: tododb
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - backend

  api:
    build: .
    ports:
      - "4444:4444"
    environment:
      DATABASE_URL: postgresql://user:pass@db:5432/tododb
    depends_on:
      - db
    networks:
      - backend

  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    networks:
      - backend

# DEFAULT_SERVER = {
#     'name': 'Clincapture Server',
#     'host': 'postgres',  # Use the service name defined in Docker Compose
#     'port': 5432,
#     'maintenance_db': 'captivate',
#     'username': 'clincapture',
#     'password': 'clincapture',
#     'sslmode': 'prefer'
# }

# SERVERS = [DEFAULT_SERVER]

networks:
  backend:

volumes:
  pgdata: